class Sepatu {
  constructor(brand, color, size) {
    this.brand = brand;
    this.color = color;
    this.size = size;
  }
  getDetails() {
    console.log(this.brand);
    console.log(this.color);
    console.log(this.size);
  }
}
